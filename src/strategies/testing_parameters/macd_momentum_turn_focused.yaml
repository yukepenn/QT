strategy: macd_momentum_turn

grid:
  features.macd_fast: [8, 12]
  features.macd_slow: [21, 26]
  signal.trigger_mode: [hist_cross_up, zero_reclaim]
  risk.stop_mode: [signal_candle_low, atr_buffer]
  risk.target_r: [1.0, 1.25]

fixed:
  features.macd_signal: 9
  features.ema_window: 20
  signal.side: long_only
  signal.atr_column: atr_like_20
  signal.entry_start_minute: 60
  signal.entry_end_minute: 270
  signal.require_price_above_ema: true
  signal.require_vwap_side: false
  signal.min_hist_slope: 0.0
  risk.target_mode: fixed_r
  risk.ema_buffer_atr: 0.1
  risk.atr_buffer_mult: 0.35
  risk.min_risk_per_share: 0.03
  risk.max_trades_per_day: 1
  backtest.eod_exit_minute: 389
  backtest.quantity: 1.0
  backtest.commission_per_trade: 0.0
  backtest.slippage_per_share: 0.01
  backtest.recompute_target_from_entry: true
  backtest.max_hold_minutes: 60
