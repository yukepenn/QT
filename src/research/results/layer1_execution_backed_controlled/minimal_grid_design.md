# Minimal balanced grids

Explicit YAMLs under **`src/strategies/testing_parameters/`** with Cartesian sizes **16 / 16 / 8**; no `--max-combos` truncation. Keys mirror focused YAMLs / strategy validators. See **`minimal_grid_design.csv`**.
