# Validation gates — next controlled Layer1 run task

See **`validation_gates.csv`** for the machine-readable matrix.

**Themes:** prove wiring before long sweeps; cap combos; keep git clean; stamp execution semantics on any promoted candidate; never treat Archive as the active config source; avoid absolute Windows drive paths in committed artifacts.
