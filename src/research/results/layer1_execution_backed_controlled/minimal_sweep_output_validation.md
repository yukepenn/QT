# Minimal sweep output validation

Each `*_minimal_proof` run folder contains `sweep_results.csv`, `sweep_meta.json`, `sweep_progress.json` (status `completed`), timing columns, merged `params_json`, and repo-relative `data_source`. No raw trade files. See **`minimal_sweep_output_validation.csv`**.
