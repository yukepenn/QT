# Minimal candidate root validation

Four `*_L1M_*.yaml` files plus index/summary CSVs; `validate-candidates` passes; `selection.candidate_kind=minimal_proof`; sources repo-relative. See **`minimal_candidate_root_validation.csv`**.
