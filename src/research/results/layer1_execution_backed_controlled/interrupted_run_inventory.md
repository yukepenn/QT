# Interrupted / superseded run inventory

Prior workspace contained **completed** `*_full_focused` sweep folders (432 / 192 / 32 rows). For this minimal-proof task they were **removed locally** so only `*_minimal_proof` runs ship in git (policy: no large full-focused Layer1 commit here). See **`interrupted_run_inventory.csv`**.
