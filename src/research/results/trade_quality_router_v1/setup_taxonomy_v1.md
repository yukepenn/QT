# Setup taxonomy v1 (research-only)

Structured mapping from **strategy family** to **setup_type** and coarse **Brooks-style regime affinity**. This is not executed by the combiner.

See `setup_taxonomy_v1.csv` for the authoritative table (≤10 setup types).

**Non-goals:** no YAML edits; no live routing; no claim that affinity columns are statistically validated until Phase-5 buckets support them.
