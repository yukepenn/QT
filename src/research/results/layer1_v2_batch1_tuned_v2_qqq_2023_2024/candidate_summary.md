# Layer 1 candidate summary (tuned v2)

**No candidates exported.**

Strict selection requires `min_profit_factor >= 1.10` (among other gates). The tuned_v2 sweep’s best row with `trades >= 40` had **profit_factor ≈ 1.048**.

See `candidate_selection_config.md` and `strategy_library_v2_batch1_tuning_v2_summary.md`.
