# Robust core dry-run — not enough candidates

Minimum suggested for a **dry-run** multi-family core (see task brief):

- ≥ **6** candidates, ≥ **2** families, no single-family dominance.

**This pack:** **2** `ROBUST_POSITIVE` candidates, **both** `cci_extreme_snapback` (one family).

**Conclusion:** do **not** publish a `robust_core_dry_run/selected_candidates_manifest.csv` as meaningful; extend the audit (remaining families + optional `full_available` column) first.
