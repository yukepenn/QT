# Diagnostic relaxed selection — DIAGNOSTIC ONLY

Rows and YAMLs under this folder are produced with **relaxed thresholds** for bottleneck analysis only.

**Do not** treat these as authoritative Layer 1 candidates or combiner inputs without repeating strict filters.

Relaxed settings: `min_trades=15`, `min_profit_factor=1.00`, `min_total_r=-3`, `max_drawdown_r=-60`, `max_avg_bars_held=150`, same EOD/EOD-data caps, `top_per_strategy=5`.
