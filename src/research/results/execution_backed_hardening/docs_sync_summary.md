# Docs sync summary

Curated rows in `docs_sync_summary.csv`. Goal: align handoff and architecture docs with **post-hardening** execution-backed Layer2 behavior (`adapter`, `state`, `ExecutionPolicy`, `materialize`, `path` scale-out).
