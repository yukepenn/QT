# Parity summary

**Status: PARITY_NOT_RUN**

Automated legacy vs canonical trade-count / total-R comparison was not executed in this task window. Synthetic unit tests cover canonical `TradeIntent` → `simulate_trade_path` behavior only.

Next: run a tiny shared precompute slice with both engines and compare aggregates (`parity_summary.csv`).
