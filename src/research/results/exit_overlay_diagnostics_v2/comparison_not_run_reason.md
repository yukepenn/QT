# comparison_not_run_reason

**Exit vs router / quality v2 comparison** was **not refreshed** in this cycle.

**Reason:** Full-panel **`ALIGNMENT_FAIL`** (`alignment_decision.md`). Per project gates, contextual overlay economics are **non-actionable** until combiner-clone replay matches panel `r_multiple` within aggregate **total R** budgets (not only mean/median |ΔR|).

**Prior** `exit_vs_router_quality_v2_comparison.*` files, if present, may reflect **pre–full-panel** or **synthetic** scaffolding — do not use them to rank exit overlay vs router until overlay reruns on a **PASS**/**PASS_WITH_WARNINGS** alignment.
