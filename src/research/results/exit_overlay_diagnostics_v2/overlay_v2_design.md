# overlay_v2_design

Contextual overlays gate **eligibility** in `exit_overlay_sim.py` before reusing the v1 bar-walk implementations.

Ambiguity: headline **`stop_first`** on combiner clone; overlay sweeps repeat with **`target_first`** / **`skip_ambiguous`**.

See `overlay_v2_design.csv`.
