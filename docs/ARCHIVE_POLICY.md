# Archive policy

Code under `archive/` is **historical reference** only. Mainline `src/` must not import it. When moving files, leave a short README in the archive folder describing origin and how to run (if ever) with explicit `PYTHONPATH`. Do not delete accounting history without archive copy.
